file(GLOB_RECURSE IGNITE_SOURCES RECURSE ${CMAKE_CURRENT_SOURCE_DIR} "*.cpp")
file(GLOB_RECURSE IGNITE_HEADERS RECURSE ${CMAKE_CURRENT_SOURCE_DIR} "*.h")

target_sources(ignite
    PUBLIC  ${IGNITE_SOURCES}
    PRIVATE ${IGNITE_HEADERS}
)
target_include_directories(ignite PUBLIC ${CMAKE_CURRENT_SOURCE_DIR})
